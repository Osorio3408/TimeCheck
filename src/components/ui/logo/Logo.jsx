import React from 'react'
// import LogoTime from "/logoTimecheck.png"

export const Logo = (props) => {
  return (
    <div>
      <img width={`${props.width}`} src={"/logoTimecheck.png"} alt="logoTimeCheck" />
    </div>
  )
}
